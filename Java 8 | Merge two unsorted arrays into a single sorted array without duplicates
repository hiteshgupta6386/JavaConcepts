Write a Java 8 program to merge two unsorted arrays into a single sorted array without duplicates.

import java.util.Arrays;
import java.util.stream.IntStream;

public class MergeArray {
    public static void main(String[] args){
        int[] array1 = {1, 2, 4, 8, 3};
        int[] array2 = {5, 9, 2, 6, 3};

        int[] mergedSortedUniqueArray = IntStream.concat(Arrays.stream(array1), Arrays.stream(array2)).sorted().distinct().toArray();

        System.out.println(Arrays.toString(mergedSortedUniqueArray));

    }
}


O/P:

[1, 2, 3, 4, 5, 6, 8, 9]
